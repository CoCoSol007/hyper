package src;

/// A set of all constant of the program
public class Constants {
    /// The tolerance used to determine an equality (a == b <-> |a - b| < TOLERANCE)
    /// because of imprecision of floating point numbers
    public static final double TOLERANCE = 0.0000001;
}
